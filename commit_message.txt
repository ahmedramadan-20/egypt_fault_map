feat: Implement Onboarding and Login screens

- Implemented Onboarding logic using `shared_preferences` and `get_it`.
- Added `OnboardingScreen` with "Skip" and "Get Started" functionality.
- Implemented `LoginScreen` with a premium blurred map background design.
- Added "Continue as Guest" and "Login" navigation to Home.
- Adjusted app logo size on Login screen.
- Updated `AppRouter` and `main.dart` to handle initial routing based on onboarding status.
