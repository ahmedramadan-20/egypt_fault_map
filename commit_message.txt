UI Overhaul & Design System Implementation

- Implemented comprehensive design system with modern color palette (AppColors) including gradients and semantic colors.
- Configured Material 3 theme in `app.dart` with custom color scheme, typography, and component themes.
- Enhanced Login and Signup screens with gradient overlays, improved card styling, and modern button designs.
- Modernized Home Screen UI:
  - Updated location permission and real-time update banners.
  - Fixed skeleton flicker issue by optimizing `HomeCubit` and `BlocListener` logic.
- Updated Fault Details, Add Fault, and Onboarding screens to align with the new design system.
- Refactored `AppTextFormField` with enhanced styling and focus states.
- Fixed `LateInitializationError` in `app.dart` by removing responsive sizing from static theme definitions.
- Fixed Android build error by enabling core library desugaring in `android/app/build.gradle.kts` for `flutter_local_notifications` support.
